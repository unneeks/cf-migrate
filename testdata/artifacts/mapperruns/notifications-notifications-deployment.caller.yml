# Caller workflow for "NOTIFICATIONS/notifications-deployment" — generated by CF Migrate.
# Calls the shared template as a reusable workflow; only variable values differ per instance.
name: notifications-deployment

on:
  workflow_dispatch: {}
  push:
    branches: [main]

jobs:
  call-template:
    uses: ./BILLING_billing-deployment.suggested.yml
    with:
      ECR_REPO: "123456789.dkr.ecr.us-east-1.amazonaws.com/notifications-app"
      HELM_RELEASE_NAME: "notifications"
      repo: "myorg/notifications-app"
      image_name: "notifications-app"
      deploy_to_staging_chart_name: "charts/notifications-app"
      deploy_to_staging_release_name: "notifications-staging"
      deploy_to_staging_replicaCount: "1"
      deploy_to_production_chart_name: "charts/notifications-app"
      deploy_to_production_release_name: "notifications-production"
      deploy_to_production_replicaCount: "3"
    secrets: inherit
