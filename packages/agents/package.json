{
  "name": "@cf-migrate/agents",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "typecheck": "tsc -b --noEmit",
    "watch": "tsc -b --watch"
  },
  "dependencies": {
    "@cf-migrate/core": "*",
    "@cf-migrate/kb": "*",
    "@cf-migrate/llm": "*",
    "yaml": "^2.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "typescript": "^5.5.4"
  }
}
